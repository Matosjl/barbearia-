# Override SÓ para validação: publica a porta do backend no host para o smoke
# test HTTP. Comunicação ENTRE containers continua por nome (postgres/redis).
# Uso: docker compose -f docker-compose.yml -f docker-compose.test.yml up -d backend
services:
  backend:
    ports:
      - "3000:3000"
